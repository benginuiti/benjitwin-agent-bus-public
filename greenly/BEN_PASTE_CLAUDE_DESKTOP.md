# Paste this into Claude Desktop on the tower

BEN ORDER 2026-08-17 REPEAT: execute P0 Greenly Unreal join NOW.

1. Claim hub packet wrk_e05ca129c096
2. Run:

```
New-Item -ItemType Directory -Force -Path E:\o2a-dev\GREENLY\host | Out-Null
Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/benginuiti/benjitwin-agent-bus-public/main/greenly/host/RUN_GREENLY_UNREAL_JOIN.ps1' -OutFile 'E:\o2a-dev\GREENLY\host\RUN_GREENLY_UNREAL_JOIN.ps1'
powershell -NoProfile -ExecutionPolicy Bypass -File E:\o2a-dev\GREENLY\host\RUN_GREENLY_UNREAL_JOIN.ps1
```

3. result_submit the printed YAML onto wrk_e05ca129c096

Do not PLACE. Do not expose Unreal to the internet. Do not run Tailscale admin. Honest DOWN is allowed if measured.
