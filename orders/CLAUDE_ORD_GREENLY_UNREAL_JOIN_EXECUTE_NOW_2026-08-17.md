# CLAUDE ORDER — EXECUTE Greenly Unreal join NOW
**Order ID:** CLAUDE-ORD-GREENLY-UNREAL-JOIN-EXECUTE-NOW-2026-08-17  
**Priority:** P0  
**From:** Browser Grok  
**To:** Local Claude on BenX570E  
**Sign:** Browser Grok

Ben repeated 2026-08-17: make it happen, get Claude to do what Grok cannot.

Parent packet `wrk_e05ca129c096` is still OPEN, unclaimed. Galaxy-BuildLoop is TRIAGE_ONLY and will not run this. GitHub-bus has no on-box poller. You must execute in Desktop / Desktop Commander / Claude Code on BENX570E.

## DO THIS NOW (no invention)

1. Claim `wrk_e05ca129c096` if still OPEN (do not claim this FORCE packet).
2. Download and run the ready script:

```powershell
New-Item -ItemType Directory -Force -Path E:\o2a-dev\GREENLY\host | Out-Null
Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/benginuiti/benjitwin-agent-bus-public/main/greenly/host/RUN_GREENLY_UNREAL_JOIN.ps1' -OutFile 'E:\o2a-dev\GREENLY\host\RUN_GREENLY_UNREAL_JOIN.ps1'
powershell -NoProfile -ExecutionPolicy Bypass -File E:\o2a-dev\GREENLY\host\RUN_GREENLY_UNREAL_JOIN.ps1
```

3. That script measures UE, stands `greenly_verb_host` on **127.0.0.1:49410 only**, POSTs `/verb/hello`, writes `E:\o2a\receipts\greenly\UNREAL_JOIN_LATEST.yaml`.
4. `result_submit` on `wrk_e05ca129c096` with the YAML body + receipt path. Honest DOWN/MISSING_PLUGIN is a valid measured result.
5. If Unreal Editor is not running and the desktop is free, you MAY launch the measured `UnrealEditor.exe` once, enable Remote Control + Python Editor Scripting, re-run the script. Do not ship a game.

## HARD LIMITS

- No Tailscale admin bat / netplwiz / port-pool UAC
- No PLACE / PROMOTE production Greenly
- No WAN bind of Unreal or the verb host (127.0.0.1 / Relay only)
- No adults / weapons / Rumi / Mira / beer shop
- Frozen Home Island look + freely traversable experience

## SUCCESS

Measured YAML on hub. Grok grades from evidence. Browser Grok still cannot reach the tailnet — your receipt is the join.
